{
  "name": "rubinot-idle-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Servidor sempre-ligado (Railway) que vai hospedar o motor de caçada autoritativo do Rubinot Idle — Marco 1: só esqueleto + verificação de sessão Supabase.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/index.js"
  }
}
