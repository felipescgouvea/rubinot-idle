node_modules/

# Conta de teste do auditor de browser — credenciais NUNCA vão pro git
# (repo é público / serve o GitHub Pages). Ver scripts/audit-browser.
.test-account.json
scripts/.wiki-cache/
reference/

scripts/*-cs-auditoria.json
__pycache__/
scripts/_*
scripts/*_preview.png

# Logs locais de trabalho dos agentes (paralelos) — NÃO vão pro git.
# finished.md = entregas de cada dreno de /backlog; defects.md = bugs achados
# pela skill test. São por-agente/por-sessão, não estado compartilhado do repo.
finished.md
defects.md
